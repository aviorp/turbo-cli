// Generated by turbo-cli at {{.Timestamp}}

import { FastifyInstance, HookHandlerDoneFunction } from 'fastify';
;

/**
 * routes will be exposed as "FastifyPlugin" approach, this is the main entry point for the routes, you can register all the routes here, assign middlewares and other configurations.
 * @param app - FastifyInstance
 * @param _ - options
 * @param done - HookHandlerDoneFunction - the "next" function in Fastify terms
 */
export function v1Routes(app: FastifyInstance, _: never, done: HookHandlerDoneFunction): void {
	app.register((api) => {
	   // create a new controller instance and register it here
		 // api.register(mockController, { prefix: '/mock' });
	});

	done();
}
